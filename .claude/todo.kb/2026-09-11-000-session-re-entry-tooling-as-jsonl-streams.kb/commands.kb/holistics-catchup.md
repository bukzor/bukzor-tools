---
status: exists
effort-sweh: 0
---

# claude-code-holistics catchup

The escalation step, unchanged: per-session digests written by dispatched
agents, for the sessions the index leaves ambiguous. Its deterministic
front half (`members.py`, `pending.py`, `render.py`) should read
[sessions] and [records] rather than re-derive them; those three are in
`../retirements.kb/`. The digest template's `span:` and `related:` fields
are copied from the sessions line at job setup.

[sessions]: ../streams.kb/sessions.md
[records]: ../streams.kb/records.md
