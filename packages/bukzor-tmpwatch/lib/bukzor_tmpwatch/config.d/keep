# Names directly inside a root that a sweep never touches, one per line.
# {boot} expands to the second this kernel booted, so a scratch directory
# named for the current boot survives however long it sits quiet.
# The quarantine directory is kept whether or not it is listed here.
# Emptied: nothing is exempt but the quarantine directory.

# default:
# boot={boot}
